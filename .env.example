# ===== TELEGRAM AI BOT - ENVIRONMENT VARIABLES =====
#
# ISTRUZIONI PER GLI STUDENTI:
# 1. Copia questo file e rinominalo in ".env" (senza .example)
#    Comando: cp .env.example .env
# 2. Compila le variabili REQUIRED con le tue API keys reali
# 3. Le variabili OPTIONAL possono rimanere commentate (usa i valori di default)
# 4. NON committare il file .env su git (è già in .gitignore)
#
# ======================================================


# ===== REQUIRED: TELEGRAM BOT =====
# Ottieni il token da @BotFather su Telegram:
# 1. Apri Telegram e cerca @BotFather
# 2. Invia il comando /newbot
# 3. Segui le istruzioni per creare il bot
# 4. Copia il token che ti viene fornito
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here


# ===== REQUIRED: OPENAI API =====
# Ottieni la API key da https://platform.openai.com/api-keys
# 1. Crea un account OpenAI (se non ce l'hai)
# 2. Vai su https://platform.openai.com/api-keys
# 3. Clicca "Create new secret key"
# 4. Copia la key (inizia con "sk-")
# NOTA: Richiede credito ($5-10 consigliati per iniziare)
OPENAI_API_KEY=sk-your_openai_api_key_here


# ===== OPTIONAL: TAVILY WEB SEARCH =====
# Ottieni la API key da https://tavily.com
# 1. Crea un account su https://tavily.com
# 2. Vai alla dashboard e copia la API key
# 3. Piano free: 1000 ricerche/mese (sufficiente per testing)
# NOTA: Se ometti questa key, il bot userà solo RAG e conoscenza diretta LLM
TAVILY_API_KEY=tvly-your_tavily_api_key_here


# ===== REQUIRED: ADMIN USER IDs =====
# Lista degli user ID Telegram che possono amministrare il bot
#
# Come ottenere il tuo Telegram user ID:
# 1. Apri Telegram e cerca @userinfobot
# 2. Invia /start al bot
# 3. Il bot ti risponderà con il tuo user ID (es. 123456789)
#
# Formato:
# - Singolo admin: ADMIN_USER_IDS=123456789
# - Multipli admin: ADMIN_USER_IDS=123456789,987654321
#
# NOTA: Gli admin possono caricare/eliminare documenti e vedere statistiche
ADMIN_USER_IDS=123456789


# ===================================================
# ===== OPTIONAL SETTINGS (valori di default) =====
# ===================================================
# Decommentare per sovrascrivere i valori di default

# --- Conversation Memory ---
# Limite token per la memoria conversazionale (default: 1500)
# MEMORY_TOKEN_LIMIT=1500

# Intervallo di salvataggio conversazioni in minuti (default: 5)
# MEMORY_SAVE_INTERVAL=5

# --- LLM Configuration ---
# Modello OpenAI da usare (default: gpt-4o-mini)
# Opzioni: gpt-4o, gpt-4o-mini, gpt-3.5-turbo
# LLM_MODEL=gpt-4o-mini

# Temperature per generazione risposte (default: 0.7)
# Range: 0.0 (deterministico) - 1.0 (creativo)
# LLM_TEMPERATURE=0.7

# --- RAG Configuration ---
# Modello per embedding documenti (default: text-embedding-3-small)
# Opzioni: text-embedding-3-small, text-embedding-3-large
# EMBEDDING_MODEL=text-embedding-3-small

# Dimensione chunks per splitting documenti in caratteri (default: 800)
# CHUNK_SIZE=800

# Overlap tra chunks consecutivi in caratteri (default: 100)
# CHUNK_OVERLAP=100

# Numero di chunks da recuperare per ogni query RAG (default: 8)
# RAG_TOP_K=8

# --- Audio Configuration ---
# Voice per Text-to-Speech OpenAI (default: alloy)
# Opzioni: alloy, echo, fable, onyx, nova, shimmer
# TTS_VOICE=alloy

# --- Agent Configuration ---
# Max iterazioni per LangChain Agent (default: 5)
# AGENT_MAX_ITERATIONS=5

# Verbose mode per debug Agent (default: False)
# Opzioni: True, False
# AGENT_VERBOSE=False
